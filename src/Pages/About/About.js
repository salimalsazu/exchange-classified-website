import React from 'react';
import useTitle from '../../Hook/useTitle';

const About = () => {

        //dynamic title
        useTitle('About')
        
    return (
        <div>

        </div>
    );
};

export default About;